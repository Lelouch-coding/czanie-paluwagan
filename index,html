<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Czanie Paluwagan</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'golden': '#FFD700',
                        'pusha-pink': '#FF69B4'
                    }
                }
            }
        }
    </script>
    <style>
        body {
            font-family: 'Arial', sans-serif;
        }
        .slide-in {
            animation: slideIn 0.3s ease-out;
        }
        @keyframes slideIn {
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        .fade-in {
            animation: fadeIn 0.3s ease-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
    </style>
</head>
<body class="bg-golden min-h-screen">
    <!-- Welcome Screen -->
    <div id="welcomeScreen" class="min-h-screen flex flex-col items-center justify-center p-4">
        <div class="text-center mb-8">
            <!-- Logo Placeholder -->
            <div class="w-24 h-24 mx-auto mb-6 bg-white rounded-full flex items-center justify-center shadow-lg">
                <svg class="w-16 h-16 text-pusha-pink" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
                </svg>
            </div>
            <h1 class="text-4xl md:text-6xl font-bold text-white mb-4 drop-shadow-lg">Czanie Paluwagan</h1>
            <p class="text-white text-lg md:text-xl mb-8 opacity-90">Your Trusted Savings Circle</p>
        </div>
        
        <div class="space-y-4 w-full max-w-sm">
            <button onclick="showLogin()" class="w-full bg-pusha-pink hover:bg-pink-600 text-white font-bold py-4 px-6 rounded-lg text-lg transition-colors shadow-lg">
                Login
            </button>
            <button onclick="showRegister()" class="w-full bg-white hover:bg-gray-100 text-pusha-pink font-bold py-4 px-6 rounded-lg text-lg transition-colors shadow-lg">
                Register
            </button>
        </div>
    </div>

    <!-- Login Screen -->
    <div id="loginScreen" class="hidden min-h-screen flex flex-col items-center justify-center p-4">
        <div class="bg-white rounded-lg shadow-xl p-8 w-full max-w-md">
            <h2 class="text-2xl font-bold text-pusha-pink mb-6 text-center">Login</h2>
            <form id="loginForm" class="space-y-4">
                <div>
                    <label class="block text-gray-700 font-bold mb-2">Username</label>
                    <input type="text" id="loginUsername" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-pusha-pink" required>
                </div>
                <div>
                    <label class="block text-gray-700 font-bold mb-2">Password</label>
                    <input type="password" id="loginPassword" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-pusha-pink" required>
                </div>
                <button type="submit" class="w-full bg-pusha-pink hover:bg-pink-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">
                    Login
                </button>
            </form>
            <button onclick="showWelcome()" class="w-full mt-4 text-pusha-pink hover:underline">Back</button>
        </div>
    </div>

    <!-- Register Screen -->
    <div id="registerScreen" class="hidden min-h-screen flex flex-col items-center justify-center p-4">
        <div class="bg-white rounded-lg shadow-xl p-8 w-full max-w-md">
            <h2 class="text-2xl font-bold text-pusha-pink mb-6 text-center">Register</h2>
            <form id="registerForm" class="space-y-4">
                <div>
                    <label class="block text-gray-700 font-bold mb-2">Full Name</label>
                    <input type="text" id="registerName" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-pusha-pink" required>
                </div>
                <div>
                    <label class="block text-gray-700 font-bold mb-2">Username</label>
                    <input type="text" id="registerUsername" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-pusha-pink" required>
                </div>
                <div>
                    <label class="block text-gray-700 font-bold mb-2">Email</label>
                    <input type="email" id="registerEmail" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-pusha-pink" required>
                </div>
                <div>
                    <label class="block text-gray-700 font-bold mb-2">Phone Number</label>
                    <input type="tel" id="registerPhone" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-pusha-pink" required>
                </div>
                <div>
                    <label class="block text-gray-700 font-bold mb-2">Password (5-10 characters, mix of letters and numbers)</label>
                    <input type="password" id="registerPassword" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-pusha-pink" required>
                    <div id="passwordStrength" class="text-sm mt-1"></div>
                </div>
                <div>
                    <label class="block text-gray-700 font-bold mb-2">Confirm Password</label>
                    <input type="password" id="confirmPassword" class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:border-pusha-pink" required>
                </div>
                <button type="submit" class="w-full bg-pusha-pink hover:bg-pink-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">
                    Register
                </button>
            </form>
            <button onclick="showWelcome()" class="w-full mt-4 text-pusha-pink hover:underline">Back</button>
        </div>
    </div>

    <!-- Main Dashboard -->
    <div id="dashboard" class="hidden min-h-screen">
        <!-- Header -->
        <header class="bg-white shadow-lg p-4">
            <div class="flex justify-between items-center max-w-6xl mx-auto">
                <h1 class="text-2xl font-bold text-pusha-pink">Czanie Paluwagan</h1>
                <div class="flex items-center space-x-4">
                    <span id="userWelcome" class="text-gray-700"></span>
                    <button onclick="logout()" class="bg-pusha-pink hover:bg-pink-600 text-white px-4 py-2 rounded-lg transition-colors">
                        Logout
                    </button>
                </div>
            </div>
        </header>

        <!-- Main Content -->
        <main class="max-w-6xl mx-auto p-4">
            <!-- Amount Selection -->
            <div id="amountSelection" class="bg-white rounded-lg shadow-xl p-6 mb-6">
                <h2 class="text-2xl font-bold text-pusha-pink mb-6 text-center">Choose Your Paluwagan Amount</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                    <div class="bg-golden p-6 rounded-lg text-center">
                        <h3 class="text-2xl font-bold text-white mb-2">₱10,000</h3>
                        <p class="text-white mb-4">₱1,000/month for 11 months</p>
                        <button onclick="selectAmount(10000, 1000)" class="bg-pusha-pink hover:bg-pink-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                            Select
                        </button>
                    </div>
                    <div class="bg-golden p-6 rounded-lg text-center">
                        <h3 class="text-2xl font-bold text-white mb-2">₱20,000</h3>
                        <p class="text-white mb-4">₱2,000/month for 11 months</p>
                        <button onclick="selectAmount(20000, 2000)" class="bg-pusha-pink hover:bg-pink-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                            Select
                        </button>
                    </div>
                    <div class="bg-golden p-6 rounded-lg text-center">
                        <h3 class="text-2xl font-bold text-white mb-2">₱30,000</h3>
                        <p class="text-white mb-4">₱3,000/month for 11 months</p>
                        <button onclick="selectAmount(30000, 3000)" class="bg-pusha-pink hover:bg-pink-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                            Select
                        </button>
                    </div>
                    <div class="bg-golden p-6 rounded-lg text-center">
                        <h3 class="text-2xl font-bold text-white mb-2">₱40,000</h3>
                        <p class="text-white mb-4">₱4,000/month for 11 months</p>
                        <button onclick="selectAmount(40000, 4000)" class="bg-pusha-pink hover:bg-pink-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                            Select
                        </button>
                    </div>
                    <div class="bg-golden p-6 rounded-lg text-center">
                        <h3 class="text-2xl font-bold text-white mb-2">₱50,000</h3>
                        <p class="text-white mb-4">₱5,000/month for 11 months</p>
                        <button onclick="selectAmount(50000, 5000)" class="bg-pusha-pink hover:bg-pink-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                            Select
                        </button>
                    </div>
                </div>
            </div>

            <!-- Selected Plan -->
            <div id="selectedPlan" class="hidden bg-white rounded-lg shadow-xl p-6 mb-6">
                <h2 class="text-2xl font-bold text-pusha-pink mb-4">Your Selected Plan</h2>
                <div class="bg-golden p-4 rounded-lg text-white">
                    <p class="text-lg"><strong>Total Amount:</strong> <span id="totalAmount"></span></p>
                    <p class="text-lg"><strong>Monthly Payment:</strong> <span id="monthlyPayment"></span></p>
                    <p class="text-lg"><strong>Duration:</strong> 11 months</p>
                    <p class="text-lg"><strong>Due Date:</strong> 30th of each month</p>
                </div>
                <button onclick="confirmJoin()" class="mt-4 bg-pusha-pink hover:bg-pink-600 text-white font-bold py-3 px-6 rounded-lg transition-colors">
                    Join Paluwagan
                </button>
            </div>

            <!-- Rules and Regulations -->
            <div class="bg-white rounded-lg shadow-xl p-6 mb-6">
                <h2 class="text-2xl font-bold text-pusha-pink mb-4">Paluwagan Rules & Regulations</h2>
                <div class="space-y-4 text-gray-700">
                    <div class="border-l-4 border-pusha-pink pl-4">
                        <h3 class="font-bold text-lg mb-2">Payment Schedule</h3>
                        <p>• Monthly payments are due every 30th of the month</p>
                        <p>• First slot goes to the handler/admin</p>
                        <p>• Subsequent slots are distributed among members</p>
                    </div>
                    
                    <div class="border-l-4 border-pusha-pink pl-4">
                        <h3 class="font-bold text-lg mb-2">Late Payment Policy</h3>
                        <p>• Grace period: 2-3 days after due date</p>
                        <p>• Penalties apply after grace period expires</p>
                        <p>• Emergency extensions may be approved by handler based on circumstances</p>
                    </div>
                    
                    <div class="border-l-4 border-pusha-pink pl-4">
                        <h3 class="font-bold text-lg mb-2">Withdrawal Policy</h3>
                        <p>• Members who back out after paluwagan starts face consequences:</p>
                        <p class="ml-4">- Pay 1 month penalty, OR</p>
                        <p class="ml-4">- No refund unless replacement member is found</p>
                        <p>• Member is responsible for finding their own replacement</p>
                    </div>
                    
                    <div class="border-l-4 border-pusha-pink pl-4">
                        <h3 class="font-bold text-lg mb-2">Account Security</h3>
                        <p>• Password must be 5-10 characters</p>
                        <p>• Must include both letters and numbers</p>
                        <p>• Keep your login credentials secure</p>
                    </div>
                </div>
            </div>

            <!-- Member Status -->
            <div id="memberStatus" class="hidden bg-white rounded-lg shadow-xl p-6">
                <h2 class="text-2xl font-bold text-pusha-pink mb-4">Your Membership Status</h2>
                <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded">
                    <p class="font-bold">Welcome to Czanie Paluwagan!</p>
                    <p>You have successfully joined the paluwagan. Your payment schedule and slot information will be provided by the administrator.</p>
                </div>
            </div>
        </main>
    </div>

    <script>
        // Global variables
        let currentUser = null;
        let selectedAmount = 0;
        let monthlyPayment = 0;

        // Screen navigation functions
        function showWelcome() {
            hideAllScreens();
            document.getElementById('welcomeScreen').classList.remove('hidden');
            document.getElementById('welcomeScreen').classList.add('fade-in');
        }

        function showLogin() {
            hideAllScreens();
            document.getElementById('loginScreen').classList.remove('hidden');
            document.getElementById('loginScreen').classList.add('slide-in');
        }

        function showRegister() {
            hideAllScreens();
            document.getElementById('registerScreen').classList.remove('hidden');
            document.getElementById('registerScreen').classList.add('slide-in');
        }

        function showDashboard() {
            hideAllScreens();
            document.getElementById('dashboard').classList.remove('hidden');
            document.getElementById('dashboard').classList.add('fade-in');
        }

        function hideAllScreens() {
            const screens = ['welcomeScreen', 'loginScreen', 'registerScreen', 'dashboard'];
            screens.forEach(screen => {
                document.getElementById(screen).classList.add('hidden');
                document.getElementById(screen).classList.remove('fade-in', 'slide-in');
            });
        }

        // Password validation
        function validatePassword(password) {
            const hasLetter = /[a-zA-Z]/.test(password);
            const hasNumber = /\d/.test(password);
            const validLength = password.length >= 5 && password.length <= 10;
            
            return {
                valid: hasLetter && hasNumber && validLength,
                hasLetter,
                hasNumber,
                validLength
            };
        }

        // Real-time password strength indicator
        document.addEventListener('DOMContentLoaded', function() {
            const passwordInput = document.getElementById('registerPassword');
            const strengthDiv = document.getElementById('passwordStrength');
            
            if (passwordInput && strengthDiv) {
                passwordInput.addEventListener('input', function() {
                    const password = this.value;
                    const validation = validatePassword(password);
                    
                    let message = '';
                    let color = 'text-red-500';
                    
                    if (password.length === 0) {
                        message = '';
                    } else if (validation.valid) {
                        message = '✓ Strong password';
                        color = 'text-green-500';
                    } else {
                        const issues = [];
                        if (!validation.validLength) issues.push('5-10 characters');
                        if (!validation.hasLetter) issues.push('letters');
                        if (!validation.hasNumber) issues.push('numbers');
                        message = `Need: ${issues.join(', ')}`;
                    }
                    
                    strengthDiv.textContent = message;
                    strengthDiv.className = `text-sm mt-1 ${color}`;
                });
            }
        });

        // Registration form handler
        document.getElementById('registerForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const name = document.getElementById('registerName').value;
            const username = document.getElementById('registerUsername').value;
            const email = document.getElementById('registerEmail').value;
            const phone = document.getElementById('registerPhone').value;
            const password = document.getElementById('registerPassword').value;
            const confirmPassword = document.getElementById('confirmPassword').value;
            
            // Validate password
            const passwordValidation = validatePassword(password);
            if (!passwordValidation.valid) {
                alert('Password must be 5-10 characters and include both letters and numbers.');
                return;
            }
            
            // Check password confirmation
            if (password !== confirmPassword) {
                alert('Passwords do not match.');
                return;
            }
            
            // Store user data (in real app, this would be sent to server)
            const userData = {
                name,
                username,
                email,
                phone,
                password
            };
            
            localStorage.setItem('userData_' + username, JSON.stringify(userData));
            alert('Registration successful! Please login with your credentials.');
            showLogin();
        });

        // Login form handler
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const username = document.getElementById('loginUsername').value;
            const password = document.getElementById('loginPassword').value;
            
            // Check stored user data
            const storedData = localStorage.getItem('userData_' + username);
            if (storedData) {
                const userData = JSON.parse(storedData);
                if (userData.password === password) {
                    currentUser = userData;
                    document.getElementById('userWelcome').textContent = `Welcome, ${userData.name}!`;
                    showDashboard();
                } else {
                    alert('Invalid password.');
                }
            } else {
                alert('User not found. Please register first.');
            }
        });

        // Amount selection
        function selectAmount(total, monthly) {
            selectedAmount = total;
            monthlyPayment = monthly;
            
            document.getElementById('totalAmount').textContent = `₱${total.toLocaleString()}`;
            document.getElementById('monthlyPayment').textContent = `₱${monthly.toLocaleString()}`;
            
            document.getElementById('amountSelection').classList.add('hidden');
            document.getElementById('selectedPlan').classList.remove('hidden');
            document.getElementById('selectedPlan').classList.add('fade-in');
        }

        // Confirm joining paluwagan
        function confirmJoin() {
            if (!currentUser) {
                alert('Please login first.');
                return;
            }
            
            // Store membership data
            const membershipData = {
                user: currentUser.username,
                amount: selectedAmount,
                monthlyPayment: monthlyPayment,
                joinDate: new Date().toISOString(),
                status: 'active'
            };
            
            localStorage.setItem('membership_' + currentUser.username, JSON.stringify(membershipData));
            
            document.getElementById('selectedPlan').classList.add('hidden');
            document.getElementById('memberStatus').classList.remove('hidden');
            document.getElementById('memberStatus').classList.add('fade-in');
        }

        // Logout function
        function logout() {
            currentUser = null;
            selectedAmount = 0;
            monthlyPayment = 0;
            
            // Reset forms
            document.getElementById('loginForm').reset();
            document.getElementById('registerForm').reset();
            
            // Hide member-specific sections
            document.getElementById('selectedPlan').classList.add('hidden');
            document.getElementById('memberStatus').classList.add('hidden');
            document.getElementById('amountSelection').classList.remove('hidden');
            
            showWelcome();
        }

        // Initialize app
        document.addEventListener('DOMContentLoaded', function() {
            showWelcome();
        });
    </script>
<script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'978e1292470ac30e',t:'MTc1NjgyNzI3Ny4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
